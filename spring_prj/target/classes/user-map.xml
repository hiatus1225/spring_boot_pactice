<?xml version="1.0" encoding="UTF-8" ?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userNameSpace">
<!-- <mapper namespace="com.biz.mapper.UserMapper"> -->
	 <!-- <resultMap id="userMap" type="com.biz.user.UserVO">
		<id property="seq" column="seq" />
		<result property="id" column="id" />
		<result property="pw" column="pw" />
		<result property="name" column="name" />
	</resultMap>  -->
	
 	<sql id="commonSelect">seq, id, pw, name</sql>
 	
 	<select id="getUser" parameterType="com.biz.user.UserVO" resultType="com.biz.user.UserVO">
 	    SELECT * FROM MVCUSER WHERE ID=#{id} AND PW=#{pw}
 <!--  jdbcType=VARCHAR -->
 	</select>
	
	
	<!-- 
	<select id="allUser" resultType="com.biz.user.UserVO">
		select  *
		from member
	</select>
	<insert id="insertUser" parameterType="com.biz.user.UserVO">
		insert into member
		values(member_seq.nextval, #{id},#{pw},#{name},'u',sysdate)
	</insert>  -->
	
</mapper>	